on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

name: Backticks

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: r-lib/actions/setup-r@v2
      - name: Test backticks
        run: R --vanilla < test-backticks.R
